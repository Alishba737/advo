# ADVO — All Dependencies

# --- Core ---
pydantic>=2.0
python-dotenv>=1.0

# --- AI/LLM ---
langgraph>=1.2
langchain>=1.3
langchain-openai>=1.6

# --- RAG Pipeline ---
qdrant-client>=1.9
httpx>=0.27

# --- API ---
fastapi>=0.141
uvicorn>=0.52
python-multipart>=0.0.32

# --- Voice (STT realtime WebSocket client; TTS uses stdlib urllib) ---
aiohttp>=3.9

# --- Document Processing ---
pymupdf>=1.24          # PDF text extraction
python-docx>=1.0       # DOCX extraction

# --- Local Embeddings (bge-m3 — runs on GPU if available, else CPU) ---
sentence-transformers>=3.0
# NOTE: The default PyPI torch wheel bundles CUDA (~3.5 GB) on Windows.
# For a CPU-only install, use PyTorch's CPU index instead:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# For NVIDIA GPU acceleration, use the CUDA index:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0
